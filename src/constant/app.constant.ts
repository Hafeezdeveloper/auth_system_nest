export enum RoleEnum {
    OPERATOR = 'operator',
    SUPER_ADMIN = 'superAdmin',
    CUSTOMER = "customer",
    DOCTOR = "doctor"
}
export const { OPERATOR, SUPER_ADMIN, CUSTOMER, DOCTOR } = RoleEnum;